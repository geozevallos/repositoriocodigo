// Es otra forma de escribir

const agregarAlCarrito = (prodcto, cantidad) => {
    /**
     * ejecución de la función
     */
}


// Si no tiene parámetros
const otraFuncion = () => {
    /**
     * Ejecuci´pon de la función
     */
}

//Si solo tiene un parametro
// const cuboDeNumero = n => {
//     return n * n * n
// }

// Lo mismo pero en una linea
const cuboDeNumero = n => (n * n * n)

console.log(cuboDeNumero(3));